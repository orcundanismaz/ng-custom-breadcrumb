import { Directive, ElementRef, Input, Attribute } from '@angular/core';

@Directive({
    selector: '[breadcrumbModel]'
})
export class BreadcrumbDirective {
  
    constructor( ) {
        
        debugger;
  }

}
